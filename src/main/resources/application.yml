spring:
    application:
          name: ticktracker-api-gateway



    cloud:
        gateway:
            server:
                webflux:
                    routes:
                      -id: ticktracker-onboard-service
                      uri:  http://localhost:8081
                      predicates:
                        -Path = /auth/**



